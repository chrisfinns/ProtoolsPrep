# Pro Tools Session Builder - Python Dependencies

# GUI Framework
PySide6>=6.6.0

# Audio file metadata (bundles libsndfile - no external sox install needed)
soundfile>=0.12.0

# Testing
pytest>=7.4.0
pytest-qt>=4.2.0

# Type hints support
typing-extensions>=4.8.0

# Pro Tools Scripting Library client (PTSL v3 = Pro Tools 2024.3)
# NOTE: on Python >=3.13 requires the protobuf-5 json_format shim
# (see prototypes/ptsl_probe.py and docs/DEVELOPER_IMPROVEMENT_PLAN.md §3.4).
# Bump in lockstep with Pro Tools upgrades: 401=2024.6, 500=2024.10, 60x=2025.6+
py-ptsl==301.0.0
